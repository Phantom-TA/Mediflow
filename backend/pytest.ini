[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = -v --tb=short --strict-markers
markers =
    unit: Unit tests (no DB required)
    integration: Integration tests (requires DB)
    slow: Slow tests (concurrency, large datasets)
